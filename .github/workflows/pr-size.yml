name: Pull request size

# docs/git-workflow.md §5 — size is never blocking, only an indicative label.
# A large pull request can be legitimate (migration, refactor); the author
# justifies it in the description.

on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  # Same clause, same race, same fix as pr-title.yml: two events in the same
  # second cancel each other out and the label is never refreshed. Nothing is
  # blocked here, only left stale, but the asymmetry would cost more to explain
  # than the cancellation ever saved.
  group: pr-size-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  label:
    name: Label by size
    runs-on: ubuntu-latest
    steps:
      - uses: pascalgn/size-label-action@v0.5.7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Indicative threshold from §5: ~400 lines of net diff.
          IGNORED: |
            /uv.lock
            /**/pnpm-lock.yaml
            /**/package-lock.json
        with:
          sizes: >-
            {
              "0":   "XS",
              "20":  "S",
              "100": "M",
              "400": "L",
              "800": "XL"
            }
