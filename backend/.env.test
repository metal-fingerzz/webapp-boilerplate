# Placeholder values: the test suite never opens a connection. They exist only
# so that Settings passes validation at import time. See docs/testing.md.
DATABASE_URL=postgresql+asyncpg://test:test@localhost:5432/test
LOG_LEVEL=warning
